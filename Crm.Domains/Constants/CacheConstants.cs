namespace Crm.Domains.Constants
{
    public static class CacheConstants
    {
        public const string AttributeCache = nameof(AttributeCache);
        public const string CategoryCache = nameof(CategoryCache);
        public const string PreferenceCache = nameof(PreferenceCache);
    }
}
